import React, { Component } from "react";
import { ToolButton } from "../../types";

interface PropTypes {
    buttons: ToolButton[]
}

// TODO: Develop this MenuBarComponent
export class MenuBarComponent extends Component<PropTypes> {
    render() {
        return (
            <div>
            </div>
        )
    }
}